# Erlang version
erlang_version=20.0

# Elixir version
elixir_version=1.5

# Always rebuild from scratch on every deploy?
always_rebuild=false

# A command to run right after compiling the app
# post_compile="mix phx.swagger.generate priv/static/swagger.json -r PartypeliWeb.Router -e PartypeliWeb.Endpoint"
